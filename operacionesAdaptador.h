#ifndef OPERACIONESADAPTADOR_H     // Si OPERACIONESADAPTADOR_H no ha sido definido
#define OPERACIONESADAPTADOR_H     // Definimos

// Añadir librerías necesarias
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

// Declaración de funciones
void modificarAdaptadorRed(FILE* archivoParam);
void copiarAdaptadorRed(FILE* archivoParam);
void addAdaptadorRed(FILE* archivoParm);

#endif      // Fin definición OPERACIONESADAPTADOR_H